<%= render(FormFieldComponent.new(label: "Webhook URL", description: "Space → Apps & integrations → Webhooks → Add webhook")) do %>
  <input type="url" name="notifier[webhook_url]" value="<%= notifier&.webhook_url %>" class="input input-bordered w-full font-mono text-sm" placeholder="https://chat.googleapis.com/v1/spaces/..." />
<% end %>
