<%= render(FormFieldComponent.new(label: "Webhook URL", description: "Channel → Connectors → Incoming Webhook → Configure")) do %>
  <input type="url" name="notifier[webhook_url]" value="<%= notifier&.webhook_url %>" class="input input-bordered w-full font-mono text-sm" placeholder="https://outlook.office.com/webhook/..." />
<% end %>
